---
# use pip version of ansible and shade only
- name: snap the instance
  command: nova image-create "{{ansible_vm.server.id}}" bhanu-test
  register: myoutput

- debug:
  var: myoutput

- name: delete instance 
  os_server:
    state: absent
    name: "{{ansible_vm.server.name}}"
